
header#header
  .navbar.navbar-fixed-top(role='navigation')
    .navbar-inner
      .container
        a.brand(href='/') Conceptor
        .nav-collapse.collapse
          ul.nav
            li
              a(href='/techniques', title="article listing") All Techniques
            li
              - if (typeof(phase) !== 'undefined'){
                a(href="/phases/" + phase._id + "/new", title="New Technique") New technique
              -}
            li
                a(href='/graph', title="graph") Graph
          ul.nav.pull-right
            if (req.isAuthenticated())
              //
                 li a(href="/users/"+req.user.id, title="Profile") Profile
              li
                a(href="/logout", title="logout") Logout
            else
              li
                a(href="/login", title="Login") Login
